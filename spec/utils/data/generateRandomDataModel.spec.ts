import 'jasmine';
import { generateRandomDataModel } from '../../../src/utils/data/generateRandomDataModel';

// TODO: write some tests to check max level of nesting object/arrays

describe('generateRandomDataModel', () => {
  beforeEach(() => {
  
  });
  
  it('', async () => {
  
  });
});